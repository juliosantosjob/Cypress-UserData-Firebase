{
  "name": "vox-tecnologia-technical-challenge",
  "version": "1.0.0",
  "description": "Desafio Técnico Vox Tecnologia",
  "main": "cypress.config.js",
  "scripts": {
    "open": "cypress open",
    "test": "cypress run",
    "lint": "npx eslint cypress",
    "allure:generate": "allure generate allure-results --clean",
    "allure:history": "if exist allure-report/history move allure-report/history allure-results/history",
    "allure:clear": "if exist allure-results rmdir /s /q allure-results && if exist allure-report rmdir /s /q allure-report",
    "allure:test": "npm run allure:clear && npm run allure:generate && npx cypress run && npm run allure:history && npm run allure:open",
    "allure:open": "npx allure serve allure-results",
    "open:stg": "cross-env ENV=stg npx cypress open ",
    "open:dev": "cross-env ENV=dev npx cypress open",
    "test:run_one": "npx cypress run --spec cypress/e2e/home.cy.js",
    "test:run_two": "npx cypress run --spec cypress/e2e/login.cy.js",
    "test:parallel": "cross-env ENV=stg npm-run-all --parallel test:run_one test:run_two"
  },
  "author": "Julio Santos",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.29.0",
    "allure-cypress": "^2.15.0",
    "cross-env": "^7.0.3",
    "cypress": "^13.6.4",
    "eslint": "^9.8.0",
    "eslint-plugin-cypress": "^3.4.0",
    "eslint-plugin-json": "^4.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "conections-fb": "^1.0.1",
    "dotenv": "^16.4.5",
    "firebase": "^10.12.4",
    "rand-select": "^1.0.0"
  }
}
